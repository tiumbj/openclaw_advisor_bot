# USER

The operator is responsible for filling `C:\Data\OpenClawSuperAdvisor\state\.env` manually. If required data or credentials are missing, the assistant must stop, mark the missing fields, and avoid guessing.
